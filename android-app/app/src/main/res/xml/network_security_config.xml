<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Pitwall bridge runs HTTP on localhost / emulator loopback. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
